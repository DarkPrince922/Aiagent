# Jarvis for Android

Нативный Android-ассистент для OpenAI-совместимого API. По умолчанию используется
`https://aiprovider.duckdns.org/v1` и модель `claude-opus-4-8`; оба значения меняются
в настройках приложения.

## Что уже работает

- чат через `POST /chat/completions`;
- локальные настройки endpoint, модели, API-ключа и system prompt;
- точная диагностика HTTP/API/TLS/сетевых ошибок и проверка подключения в настройках;
- очередь запросов в SQLite и повтор через WorkManager после восстановления сети;
- многошаговый agent loop с возвратом результатов инструментов модели;
- режим агента без лимита шагов с ручной остановкой и защитой от повторяющихся циклов;
- автономная задача останавливается сама, если перестала продвигаться: три хода подряд
  текстом без единого действия или исчерпанный потолок шагов. Причина уходит в журнал, в чат и в
  уведомление — иначе задача способна отвечать по кругу всю ночь, пока пользователь спит;
- эта остановка отключается переключателем «Останавливать застрявшую задачу», а её потолок шагов
  (20–400, по умолчанию 60) задаётся слайдером рядом. С выключенной остановкой задача идёт до
  `finish_task` или до кнопки «Стоп»: вместо остановки агент получает жёсткое напоминание —
  вызвать инструмент или завершить задачу, — а в журнале появляется предупреждение «Агент топчется
  на месте»;
- отдельные автономные задачи: работа до проверенного результата, pause/stop и журнал ключевых событий;
- автономная задача продолжается при закрытом приложении: WorkManager держит её в foreground-сервисе,
  уведомление показывает текущий шаг и даёт кнопки «Стоп» и «Пауза» прямо из шторки;
- уведомление о результате: завершение, остановка и ошибка приходят, даже если приложение закрыто;
- вмешательство по ходу: указание, отправленное запущенной задаче, попадает в диалог перед следующим
  шагом — контекст и checkpoint сохраняются, работа не прерывается; для остановленной или завершённой
  задачи указание возвращает её в работу;
- checkpoint после каждого шага и автоматическое возобновление WorkManager после обрыва сети или перезапуска приложения;
- несколько сохраняемых чатов с переключением и удалением истории;
- Google News/Bing RSS/DuckDuckGo Lite search с fallback и устойчивое чтение публичных HTTPS-страниц:
  повтор при таймаутах и 5xx, поддержка не-HTML ответов, понятная причина отказа вместо общей ошибки;
- SSH-профили, password/private-key auth, pinning fingerprint и удалённые команды;
- удалённый SSH operation ledger: после reconnect агент опрашивает ту же операцию и не повторяет команду вслепую;
- заметки, HTTP/DNS/JSON/SHA-256/Base64/UUID, калькулятор и статус устройства;
- браузер, карты, набор номера, письма, календарь, таймеры, share и clipboard;
- голосовой ввод, каталог инструментов, тёмная terminal-тема и анимированные переходы;
- выбор инструмента подставляет редактируемое задание в черновик и не отправляет его сам;
- в обычном чате SSH и внешние действия требуют подтверждения; автономная задача не спрашивает
  подтверждений и работает по гранту: один выбранный SSH-профиль плюс HTTP-запросы любым методом,
  включая POST. Всё, что вне гранта, инструмент отклоняет сам, не останавливая задачу;
- API-ключ и SSH-секреты шифруются AES-GCM ключом из Android Keystore.

## Свой сервер

В поле endpoint можно указать адрес собственного сервера с OpenAI-совместимым API
(Ollama, llama.cpp server, vLLM). Для адреса в локальной сети разрешён обычный `http://`:

```
http://192.168.1.50:11434/v1
```

Наружу по-прежнему только `https://` — незашифрованный API-ключ в интернете недопустим.
Проверка выполняется по фактически разрешённому IP, а не по виду строки.

## Обмен файлами с агентом

Агент умеет принимать и отдавать текстовые и JSON-файлы.

- **Передать файл агенту**: скрепка в строке ввода или «Поделиться» → Jarvis. Файл попадает
  в рабочую папку приложения, разрешения не нужны. Поддерживаются `.txt`, `.json`, `.md`,
  `.csv`, `.log`, `.yaml`, `.xml`, `.sql`, а также `.zip` и базы `.db` до 4 МБ. Тип файла при
  выборе не фильтруется: систему нельзя заставить одинаково называть `.sql` и `.db`, она
  отдаёт их то как `application/sql`, то как `application/octet-stream`, и по типам они
  просто не выбирались бы.
- **Прикрепить файл к автономной задаче**: кнопка есть и в форме новой задачи, и в поле
  «Вмешаться по ходу». Файл кладётся в рабочую папку, а его имя дописывается в текст цели
  или указания — без имени агент не поймёт, какой из лежащих там файлов относится к делу.
- **Получить файл от агента**: в чате `send_file` открывает системное меню отправки.
  В автономной задаче так нельзя — Android 10+ запрещает фоновый запуск активности, а
  задача работает как раз при закрытом приложении. Поэтому оттуда файл приходит
  уведомлением: нажатие открывает то же меню отправки, и подтверждения агент не ждёт.
- Автономная задача видит рабочую папку, но не весь её ворох: в служебный блок попадают
  файлы, добавленные незадолго до постановки задачи, — это и есть то, что вы к ней
  прикрепили. Остальное упоминается числом с явной пометкой, что к этой задаче оно не
  относится. Полный список агент возьмёт через `list_files`, если цель этого требует.
- Поддерживаются также `.zip` и базы `.db`, `.sqlite`, `.sqlite3` — двоичные, текстом не читаются.
- **Файл на сервер и обратно**: `upload_file` и `download_file` работают по SFTP на том же
  профиле, что закреплён за задачей. Через `ssh_exec` файл не передать — содержимое пришлось
  бы вставлять в команду, а её лимит 8000 символов; отчёт туда не помещается.
- Инструменты: `list_files`, `read_file`, `search_file`, `write_file`, `send_file`, `create_zip`.
- **Архивы**: `create_zip` собирает zip из файлов рабочей папки — десять файлов уходят одним
  вложением, а не десятью. Архив можно отправить (`send_file`) и положить на сервер
  (`upload_file`), но не прочитать: `read_file` работает только с текстом, а zip в контексте
  модели — это сотни килобайт мусора. `.zip` — единственное двоичное расширение, которое
  переживает нормализацию имени; всё прочее неизвестное по-прежнему становится `.txt`.
- Начало каждого прикреплённого файла попадает прямо в сообщение — модель видит содержимое
  даже до первого вызова инструмента.

**Большие файлы.** Отчёт на сотни килобайт в один ответ модели не помещается, поэтому
`read_file` читает окно и сообщает координаты: общий размер, показанный диапазон и смещение
следующего куска. Агент дочитывает файл повторными вызовами или прыгает к нужному месту
через `search_file`, который возвращает номера строк с совпадениями. Молчаливой обрезки
больше нет — модель всегда видит, сколько осталось.

Имя файла приходит от модели и считается недоверенным: каталоги отбрасываются целиком,
расширение приводится к текстовому, выйти за пределы рабочей папки невозможно.

## Базы данных

SQLite-база из рабочей папки открывается как база, а не как текст: `read_file` на двоичном
файле бесполезен, а через `ssh_exec` таблицу пришлось бы вытаскивать разбором чужого вывода.

- `sql_import` — заливает дамп `.sql` в базу: дамп это текст, а не таблица, и посчитать по
  нему ничего нельзя, пока он не импортирован. Ошибочный оператор не прерывает импорт —
  дампы часто содержат директивы MySQL или PostgreSQL, и терять из-за них все таблицы
  бессмысленно; что не применилось, видно в отчёте;
- `sql_schema` — таблицы, число строк и DDL; с этого начинают работу с незнакомой базой;
- `sql_query` — чтение (`SELECT`, `WITH`, `PRAGMA`, `EXPLAIN`) с лимитом строк;
- `sql_exec` — изменение (`INSERT`, `UPDATE`, `DELETE`, `CREATE`) с числом затронутых строк;
- `sql_export` — выгрузка результата в CSV или Markdown, дальше `send_file` или `upload_file`.

Работает и в чате, и в автономной задаче. В чате изменение базы требует подтверждения; в
автономной задаче оно идёт по гранту — файл лежит в её же рабочей папке, а ждать подтверждения
ей нечем.

Читающие и изменяющие запросы разделяются по первому слову, а не поиском подстрок: `SELECT *
FROM updates` ничего не меняет, хотя и содержит «update». Несколько операторов через `;`
отклоняются — иначе «читающий» запрос протащил бы за собой `DELETE`. База открывается в
режиме READONLY там, где обещано только чтение, и только по нормализованному имени внутри
рабочей папки.

Путь от дампа к таблице целиком: прикрепите `.sql` в чат (или заберите с сервера через
`download_file`) → `sql_import` → `sql_query` для выборки → `sql_export` в Markdown →
`send_file`. Разбор дампа не наивный: точка с запятой внутри строкового литерала, внутри
комментария и внутри тела триггера `BEGIN … END` не разрывает оператор.

Базу с сервера забирают через `download_file`, изменённую возвращают через `upload_file`.

## Параллельные задачи

Одновременно считаются 1–4 задачи, число задаётся в настройках (по умолчанию 3). Остальные
ждут очереди со статусом «Ожидает очереди» и стартуют по мере освобождения слота.

Задачи изолированы:

- **своя папка файлов** — `workspace/tasks/<id>`. Пишет задача только к себе, читает сначала
  у себя, а потом в общей папке, где лежат ваши вложения. Две задачи, записавшие `report.md`,
  больше не затирают друг друга; удаляет задача тоже только своё;
- **свои SSH-операции** — идентификатор операции содержит id задачи, поэтому каталог журнала
  и tmux-сессия у каждой свои даже при одинаковых командах;
- **свой чат, свой checkpoint, свой журнал событий**.

Папка задачи удаляется вместе с задачей. Пул потоков WorkManager выделен под максимум задач
плюс запас, чтобы очередь отправки и загрузка модели не вставали за длинными задачами.

Чего изоляция не даёт: задачи ходят на сервер под одним SSH-профилем, поэтому если две из них
трогают один и тот же файл или сервис на хосте, они столкнутся там — это уже вне приложения.
Лимиты провайдера тоже общие.

## Файлы агента

Вкладка «Файлы» показывает рабочую папку целиком: отчёты, архивы, всё, что агент собрал через
`write_file`, `create_zip` или `download_file`, и всё, что вы прикрепили в чате. Нажатие
открывает начало текстового файла, кнопка сохраняет его через системное меню — в загрузки,
в облако, куда угодно. Значок с метлой чистит папку целиком: копившиеся отчёты прежних
запусков — главная причина, по которой автономная задача начинает читать чужое и путаться. Архив не показывается текстом: его отправляют, а не читают. Список
перечитывается при каждом входе, поэтому свежий отчёт видно сразу.

## Настройка промтов

Все тексты, которые приложение добавляет от себя, редактируются в Настройках → «Промты»:
правило «сначала ответ, потом инструменты», описание инструментов и файлов, правила
автономной задачи и запрос на итог. У каждого поля своя кнопка «Сбросить», внизу — сброс
всех сразу. Пустое поле означает «вернуть исходный текст», а не «отправлять пустоту».

**Имя и характер ассистента задаются только системной инструкцией.** В промте по умолчанию
имени нет намеренно: иначе на «ты кто?» модель отвечает так, как решил автор приложения,
а не так, как задали вы. Название Jarvis остаётся только в интерфейсе — в уведомлениях и
запросах подтверждения, то есть там, где его читаете вы, а не модель.

## Порядок работы агента

По умолчанию включён режим «сначала ответ, потом действия»: модель обязана сперва написать
текстом, что поняла и что собирается делать, и только затем вызывать инструменты. Этот текст
появляется в чате отдельным сообщением **до** выполнения команд, так что видно намерение, а
не только результат. Отключается в настройках.

Правило не только объявляется, но и проверяется. В чате: если на первом шаге модель молча
вызвала инструмент, запрос повторяется с явным требованием сперва ответить (у локального
движка проверка выключена — лишний запрос там стоит минут). В автономной задаче первый шаг
уходит вообще без схем инструментов, поэтому ответ с планом обязан появиться раньше любых
действий и сразу дублируется в чат.

## Основная инструкция не теряется

Инструкция из настроек уходит **дословно, отдельным системным сообщением, без единой
приписки**. Всё, что дописывает приложение — протокол ответа, дата, список SSH-профилей,
описание рабочей папки, — идёт вторым сообщением с явной пометкой, что оно подчинено
основной инструкции. Большая облачная модель разделяет их и в одном блоке, небольшая своя —
нет: читает наравне и отвечает по смеси.

**Знакомство в три хода** (Настройки → «Знакомство в начале чата», включено по умолчанию):

1. модель получает только инструкцию, без инструментов, и отвечает на неё;
2. получает служебные правила вместе со схемами инструментов и отвечает на них;
3. и только теперь приходит задание пользователя.

**Если инструкция всё равно не работает**, проверьте, доходит ли она вообще: Настройки →
«Проверить, доходит ли инструкция». Приложение отправит её так же, как в обычном чате, и
попросит модель процитировать полученное. Ответ разделяет два случая, которые снаружи
неотличимы: приложение отправило не то — или сервер подменил системное сообщение
собственным промтом, навязав модели «официальную» личность. Так делают многие прокси.

Во втором случае помогает переключатель «Дублировать инструкцию сообщением пользователя»:
инструкция уходит ещё и обычным сообщением, которое прокси, как правило, не трогают. Текст
остаётся полностью вашим — приложение не добавляет к нему ни слова.

Приложение при этом не говорит ни слова от себя: ни «прочитай», ни «подтверди» — на
системное сообщение модель отвечает и без подсказки, а лишняя реплика была бы такой же
припиской, от которых всё и уходит. В запросе на первом ходе нет ни одной схемы инструментов.

Оба ответа остаются в контексте репликами ассистента — собственные слова держат модель
сильнее любого текста от приложения. Ответы берутся один раз на диалог: первый
переспрашивается при правке инструкции, второй — ещё и при изменении набора инструментов.

Исход запоминается всегда, включая неудачный: сбой знакомства не блокирует сообщение и не
повторяется на каждой реплике. Если сервер не принимает запрос из одних системных сообщений
(некоторым нужен ход пользователя), приложение один раз повторяет его, отправив инструкцию
ещё и сообщением пользователя — это по-прежнему ваш текст, приложение своего не добавляет.

При переполнении контекста режется служебный блок и хвост переписки, инструкция — никогда,
а вступление сохраняется целиком. Когда ход вырастает (много сообщений или один результат
инструмента на сотни килобайт), инструкция повторяется напоминанием перед ответом. Запросы
за итогом и финальным синтезом получают её всегда: именно этот текст читает пользователь.
Напоминания уходят только в запрос, в истории чата их нет.

## Локальная модель (офлайн)

В настройках переключатель «Облако / Локально». Локальный движок — llama.cpp, собирается из
исходников на этапе сборки (CMake FetchContent, тег `b10259`), только под `arm64-v8a`.

Модель скачивается прямо из приложения: Настройки → Движок → Локально → «Скачать» напротив
нужного варианта. Ни компьютер, ни файловый менеджер не нужны.

Файл кладётся в собственный каталог приложения (`Android/data/app.jarvis/files/models`) —
именно поэтому не требуется ни одного разрешения на доступ к памяти: `/sdcard/Download`
приложению на Android 11+ уже недоступен.

Загрузка идёт в foreground-сервисе, поэтому переживает сворачивание приложения, показывает
прогресс в уведомлении и возобновляется с прерванного места после обрыва связи (HTTP Range),
а не начинает гигабайты заново. Перед стартом проверяется свободное место.

Рекомендуется Qwen3 4B Q4_K_M (~2.5 ГБ) — в своём классе заметно лучше остальных в tool calling.
Рядом есть поле для своей ссылки на любой `.gguf`. Там же настраиваются контекст и число потоков
(0 — подобрать по числу ядер; больше потоков, чем больших ядер, только замедляет).

Вызовы инструментов ограничены GBNF-грамматикой, которая строится из тех же JSON-схем, что
уходят в облако: модель физически не может выдать несуществующее имя инструмента или сломанный
JSON. Грамматика ленивая — свободный текст не ограничен, правила включаются только после того,
как модель сама начала `<tool_call>`.

Ограничения, о которых стоит знать: контекст на устройстве на порядок меньше облачного (история
подрезается автоматически), длинный вывод инструментов обрабатывается минутами, а телефон при
непрерывной нагрузке уходит в троттлинг. Для долгих автономных задач с SSH локальный движок
не предназначен — это режим для офлайн-чата и простых локальных инструментов.

## Запуск

Откройте корневую папку в Android Studio, дождитесь Gradle Sync и запустите модуль
`app` на Android 8.0 или новее. В настройках приложения укажите API-ключ провайдера.
Из терминала: `./gradlew :app:assembleDebug` — версия Gradle зафиксирована в wrapper.

Разрешите уведомления при первом запуске: на Android 13+ без них не будет видно ни прогресс
автономной задачи, ни кнопок управления в шторке.

## Сборка на GitHub

Workflow `.github/workflows/android.yml` собирает debug APK после каждого push в
`main`, для pull request и при ручном запуске. Готовый файл находится на странице
запуска GitHub Actions в артефакте `jarvis-debug-apk`.

Debug APK подписывается публичным development-ключом `app/jarvis-debug.keystore`,
чтобы последующие тестовые сборки устанавливались как обновления. Этот ключ нельзя
использовать для production-релиза или публикации в Google Play.

Проект намеренно не хранит API-ключи и SSH-секреты в репозитории. Все сетевые инструменты
(`web_fetch`, `web_search`, `http_request`, `open_url`) проходят через единую политику `UrlPolicy`:
разрешён только HTTPS, запрещены credentials в URL и любые адреса, резолвящиеся в loopback,
RFC1918, link-local, CGNAT или IPv6 ULA — включая каждый шаг цепочки редиректов. Модель видит только
ID и публичные параметры SSH-профиля; пароль и private key в контекст не передаются и не
расшифровываются при обычном показе списков.

**tmux.** Команды автономной задачи запускаются в отдельной tmux-сессии `jarvis-<операция>`,
поэтому переживают обрыв связи и доступны для наблюдения: `tmux attach -t jarvis-…`. Имя
сессии возвращается в ответе инструмента. После реконнекта агент опрашивает ту же операцию по
её идентификатору и подхватывает результат, а не запускает команду заново. Если tmux на
сервере нет или сессию создать не удалось, работает прежний путь через `nohup` — молча и без
потери функциональности.

Для возобновляемых SSH-операций на тестовом сервере нужны POSIX `sh`, `base64`, `nohup`, `dd`, `wc`,
`tr` и `find`. Команда выполняется из `$HOME` — так же, как в обычном чате; журнал операций лежит
в `~/.cache/jarvis-agent/ops` и автоматически чистится от записей старше 7 дней.
Команды запускаются без PTY, поэтому `sudo` должен быть non-interactive и ограничен нужными для теста командами.
